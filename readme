# 🎯 Demonstração Interativa - Tags HTML

> Este repositório tem o intuito de resumir as aulas do dev em dobro com exemplos práticos.

## 📋 Sobre o Projeto

Este projeto é uma demonstração interativa e visual das principais tags HTML usadas para criar formulários e capturar dados do usuário. Cada exemplo inclui código, explicação e feedback em tempo real!

## 🚀 Acesse o Projeto

🔗 **[Ver Demonstração ao Vivo](https://pavaliek.github.io/Tipos_tag_HTML/)**

## 🏷️ Tags Abordadas

### 1️⃣ **Input** (`<input>`)
A tag `<input>` cria campos de entrada de dados. É uma das tags mais versáteis do HTML!

**Tipos demonstrados:**
- `type="text"` - Campo de texto simples
- `type="email"` - Campo de email com validação
- `type="password"` - Campo de senha (oculta caracteres)
- `type="number"` - Campo numérico
- `type="checkbox"` - Caixas de seleção
- `type="radio"` - Botões de opção

### 2️⃣ **ID** (atributo)
O atributo `id` identifica um elemento de forma única na página.

**Para que serve:**
- Identificar elementos no JavaScript
- Estilizar elementos específicos no CSS
- Criar âncoras de navegação

**Exemplo:**
```html
<input type="text" id="nome">
```

### 3️⃣ **Placeholder** (atributo)
O atributo `placeholder` exibe um texto de dica dentro do campo que desaparece ao digitar.

**Para que serve:**
- Orientar o usuário sobre o que deve ser digitado
- Melhorar a experiência do usuário (UX)
- Fornecer exemplos de formato esperado

**Exemplo:**
```html
<input type="email" placeholder="seuemail@exemplo.com">
```

### 4️⃣ **Select** (`<select>`)
A tag `<select>` cria menus dropdown (listas suspensas).

**Recursos:**
- `<option>` - Define cada opção do menu
- `<optgroup>` - Agrupa opções em categorias
- `selected` - Marca opção pré-selecionada
- `multiple` - Permite seleção múltipla

**Exemplo:**
```html
<select id="pais">
  <option value="">Escolha um país</option>
  <option value="br">Brasil</option>
  <option value="pt">Portugal</option>
</select>
```

### 5️⃣ **Textarea** (`<textarea>`)
A tag `<textarea>` cria campos de texto com múltiplas linhas.

**Para que serve:**
- Comentários e reviews
- Mensagens de contato
- Descrições longas
- Biografias

**Atributos importantes:**
- `rows` - Número de linhas visíveis
- `cols` - Largura em caracteres
- `maxlength` - Limite de caracteres
- `placeholder` - Texto de dica

**Exemplo:**
```html
<textarea id="mensagem" rows="5" placeholder="Digite sua mensagem..."></textarea>
```

## 💡 Recursos Interativos

✨ **Feedback em Tempo Real** - Veja os valores capturados instantaneamente

📊 **Estatísticas Dinâmicas** - Contador de caracteres, palavras e linhas

🎨 **Design Moderno** - Interface responsiva e visualmente atrativa

📱 **100% Responsivo** - Funciona perfeitamente em todos os dispositivos

## 🛠️ Tecnologias Utilizadas

- **HTML5** - Estrutura semântica
- **CSS3** - Estilização moderna com gradientes e animações
- **JavaScript** - Interatividade e manipulação do DOM

## 📚 O Que Você Vai Aprender

- ✅ Como usar diferentes tipos de inputs
- ✅ Diferença entre `id`, `name` e `class`
- ✅ Quando usar `input` vs `textarea`
- ✅ Como organizar opções com `<select>` e `<optgroup>`
- ✅ Validação básica de formulários
- ✅ Atributos úteis como `required`, `disabled`, `readonly`
- ✅ Como capturar valores com JavaScript

## 🎓 Casos de Uso Práticos

| Tag | Caso de Uso |
|-----|-------------|
| `<input type="text">` | Nome, endereço, busca |
| `<input type="email">` | Cadastro, login, newsletter |
| `<input type="password">` | Login, cadastro, alteração de senha |
| `<input type="number">` | Idade, quantidade, preço |
| `<input type="checkbox">` | Aceitar termos, preferências múltiplas |
| `<input type="radio">` | Escolher uma opção única |
| `<select>` | País, estado, categoria |
| `<textarea>` | Comentários, mensagens, descrições |

## 📖 Como Usar Este Projeto

1. **Clone o repositório:**
```bash
git clone https://github.com/pavaliek/Tipos_tag_HTML.git
```

2. **Abra o arquivo index.html no navegador:**
```bash
cd Tipos_tag_HTML
# Abra o index.html no seu navegador
```

3. **Explore e teste!** Interaja com todos os exemplos

## 🤝 Contribuições

Contribuições são bem-vindas! Sinta-se à vontade para:
- Reportar bugs
- Sugerir novos exemplos
- Melhorar a documentação
- Adicionar mais tags HTML

## 📝 Licença

Este projeto é de código aberto e está disponível para fins educacionais.

## 👩‍💻 Autora

Desenvolvido com 💜 por [pavaliek](https://github.com/pavaliek)

---

⭐ **Se este projeto foi útil, deixe uma estrela!**

📚 **Parte do curso Dev em Dobro**
